# Private Discriminator Resources

These CSV files are downloadable module indexes for LookInside. They keep the
same schema as `PrivateDiscriminatorCSV` and use one file per Swift module.

LookInside may fetch these files from the repository raw URLs and store updated
copies in its local `private_disc/imported/` or managed resource cache.
